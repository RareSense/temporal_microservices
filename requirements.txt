fastapi==0.111.0
uvicorn[standard]==0.29.0
torch==2.3.0           # or CPU-only torch==2.3.0+cpu
timm==1.0.3
pillow>=10
python-magic-bin; sys_platform == "win32"
python-magic; sys_platform != "win32"
azure-storage-blob>=12.20.0     # only if you need artifact downloads
